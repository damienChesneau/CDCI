package main

import (
	"fmt"
	//"strings"
)

func main() {
	//var v string = "Hello Damien"
	//i := 0
	//fmt.Println("%s", strings.HasPrefix(v, "Hello"))
	for h, i := range [] int {1, 2, 3, 4} {
		fmt.Println(h,i)
	}
}
